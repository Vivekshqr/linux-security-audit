# Custom Security Checks Configuration
# Add custom commands to run during the audit
# Example: check for specific file existence
[ -f /etc/important_config ] && echo "Important config exists" || echo "Important config missing"

# Example: check for specific package
dpkg -l | grep -q nginx && echo "Nginx installed" || echo "Nginx not installed"